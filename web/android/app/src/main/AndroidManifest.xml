<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <!-- Talking to the teacher's server over the LAN. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lets the app tell "the wifi is off" apart from "the server is down",
         which is the difference between two very different Arabic errors on
         the «إعداد الخادم» screen. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      PACKAGE VISIBILITY — CRITICAL, DO NOT DELETE.

      From Android 11 (API 30) an app can only see other packages it declares
      here. Without this block `queryIntentActivities` returns nothing for a
      wa.me URL, so the whole Tier 0 flow dies silently: «فتح واتساب» appears
      to do nothing at all — no crash, no error, no WhatsApp. It is the single
      most common reason a Capacitor build works in the browser and fails on a
      real phone.

      The two packages are consumer WhatsApp and WhatsApp Business (many
      teachers use Business for parents). The <intent> entry additionally makes
      any https handler visible, so the fallback to a browser still works on a
      device where neither WhatsApp is installed.
    -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
